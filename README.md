# 🖱️ MouseDetector (Cross-Platform C++17 Project)

A simple, extensible cross-platform mouse (and input device) detection system built in **C++17** with **CMake**.

Currently supports:
- ✅ Linux (real detection using `libudev`)
- 🧩 Windows & macOS (simulation mode for now)
- 🧱 CMake build system
- 📦 Ready for extension as a library

---

## 🚀 Build Instructions

### 1️⃣ Clone
```bash
git clone https://github.com/Cally6546/MouseDetector.git
cd MouseDetector
